spring.application.name=paymentservice
server.port=8886

# ------------------------------
# PostgreSQL Configuration
# ------------------------------
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5433/restaurantservice}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:thanh0909246319}
spring.datasource.driver-class-name=org.postgresql.Driver

# ------------------------------
# JPA/Hibernate Configuration
# ------------------------------
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true


# ------------------------------
# KAFKA & AVRO CONFIGURATION (Docker Environment)
# ------------------------------

# --- Consumer Configuration ---
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.apache.kafka.common.serialization.StringDeserializer


#payment-service.kafka.payment-response-topic=order.payment.response
restaurant-service.kafka.restaurant-response-topic=restaurant.approval.response

#---------------------------ListenTopicKafka----------------------
restaurant-service.kafka.listen-order-state-topic =order.state.topic

spring.kafka.consumer.group-id=restaurant-service-consumer-group
