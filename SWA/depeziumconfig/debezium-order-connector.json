{
    "name": "orderservice-outbox-connector",
    "config": {
        "connector.class": "io.debezium.connector.postgresql.PostgresConnector",
        "database.hostname": "postgres_order",
        "database.port": "5432",
        "database.user": "postgres",
        "database.password": "thanh0909246319",
        "database.dbname": "orderservice",
        "database.server.name": "orderservice-db-server", 
        "topic.prefix": "dbz.orderservice", 
        "table.include.list": "order.order_outbox",
        "publication.name": "dbz_order_outbox_publication", 
        "plugin.name": "pgoutput", 
        "transforms": "outbox",
        "transforms.outbox.type": "io.debezium.transforms.outbox.EventRouter",
        "value.converter.schemas.enabled": "true",
        "transforms.outbox.table.field.event.key": "saga_id",
        "transforms.outbox.table.field.event.payload": "payload",
        "transforms.outbox.route.by.field": "event_type",
        "transforms.outbox.route.topic.replacement": "${routedByValue}"
        
    }
}